name: go-lint
on:
  push:
    branches:
      - master
      - v**
  pull_request:
    branches:
      - master
      - v**

jobs:
  golangci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: run lint
        run: make go-lint
